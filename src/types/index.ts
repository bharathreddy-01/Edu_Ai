export type NavItem = {
  title: string;
  href: string;
  disabled?: boolean;
};

export type LearningLevel = "beginner" | "intermediate" | "advanced";
export type LearningMode = "tutoring" | "quiz" | "evaluation" | "planning";
export type DifficultLevel = "easy" | "medium" | "hard" | "expert";
